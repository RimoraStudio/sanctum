apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanctum-gateway.fullname" . }}
  labels:
    {{- include "sanctum-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sanctum-gateway.selectorLabels" . | nindent 4 }}

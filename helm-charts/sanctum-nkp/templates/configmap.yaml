{{- /*
config.siteUrl is mandatory. Sanctum uses SITE_URL to lock down CORS and to
build every absolute URL it emits (invite / password-reset / verification
links, SSO and OAuth redirect URIs, WebAuthn relying party). Fail the install
early with a clear message rather than deploying a silently degraded instance.
*/ -}}
{{- $siteUrl := required "config.siteUrl is required: set it to the external URL users will reach Sanctum at, e.g. https://sanctum.example.com" .Values.config.siteUrl -}}
{{- if not (regexMatch "^https?://[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?(\\.[A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?)*(:[0-9]{1,5})?$" $siteUrl) -}}
{{- fail (printf "config.siteUrl must be a bare origin of the form http(s)://<hostname>[:port] with no path, query, fragment or credentials (Sanctum cannot be served under a path prefix); got %q" $siteUrl) -}}
{{- end -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sanctum.fullname" . }}-config
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
data:
  SITE_URL: {{ $siteUrl | quote }}
  PORT: {{ .Values.config.port | quote }}
  TELEMETRY_ENABLED: {{ .Values.config.telemetryEnabled | quote }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanctum.fullname" . }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sanctum.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

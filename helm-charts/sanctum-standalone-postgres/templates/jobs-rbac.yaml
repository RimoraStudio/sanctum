---
{{- if .Values.sanctum.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ printf "%s-sanctum" .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
  {{- with .Values.sanctum.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "sanctum.roleName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
rules:
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["get", "watch", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "sanctum.roleBindingName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "sanctum.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "sanctum.roleName" . }}
---
{{- if .Values.sanctum.autoBootstrap.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "sanctum.roleName" . }}-bootstrap
  namespace: {{ .Values.sanctum.autoBootstrap.secretDestination.namespace | default .Release.Namespace }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "sanctum.roleBindingName" . }}-bootstrap
  namespace: {{ .Values.sanctum.autoBootstrap.secretDestination.namespace | default .Release.Namespace }}
  labels:
    {{- include "sanctum.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "sanctum.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "sanctum.roleName" . }}-bootstrap
{{- end }}

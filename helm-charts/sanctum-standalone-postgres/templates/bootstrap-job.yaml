{{- $sanctumValues := .Values.sanctum }}
{{- if $sanctumValues.autoBootstrap.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-bootstrap-{{ .Release.Revision }}"
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "10"
    "helm.sh/hook-delete-policy": before-hook-creation
  labels:
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  backoffLimit: 3
  template:
    metadata:
      name: "{{ .Release.Name }}-bootstrap"
      labels:
        app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
        app.kubernetes.io/instance: {{ .Release.Name | quote }}
        helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    spec:
      serviceAccountName: {{ include "sanctum.serviceAccountName" . }}
    {{- with $sanctumValues.podSecurityContext }}
      securityContext: {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- if $sanctumValues.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml $sanctumValues.image.imagePullSecrets | nindent 6 }}
    {{- end }}
      restartPolicy: OnFailure
      initContainers:
      - name: wait-for-sanctum
        image: curlimages/curl:8.14.1
        {{- with $sanctumValues.containerSecurityContext }}
        securityContext: {{- toYaml . | nindent 10 }}
        {{- end }}
        command: ['sh', '-c']
        args:
        - |
          echo "Waiting for Sanctum to be ready..."
          until curl -f http://{{ include "sanctum.fullname" . }}:8080/api/status; do
            echo "Sanctum not ready yet, retrying in 10 seconds..."
            sleep 10
          done
          echo "Sanctum is ready! Proceeding with bootstrap..."
      containers:
        - name: sanctum-bootstrap
          image: "sanctum/cli:{{ $sanctumValues.autoBootstrap.image.tag }}"
          imagePullPolicy: {{ $sanctumValues.image.pullPolicy | default "IfNotPresent" }}
          {{- with $sanctumValues.containerSecurityContext }}
          securityContext: {{- toYaml . | nindent 12 }}
          {{- end }}
          args:
            - bootstrap
            - --domain=http://{{ include "sanctum.fullname" . }}:8080
            - --output=k8-secret
            - --k8-secret-name={{ $sanctumValues.autoBootstrap.secretDestination.name }}
            - --k8-secret-namespace={{ $sanctumValues.autoBootstrap.secretDestination.namespace | default .Release.Namespace }}
            - --organization={{ $sanctumValues.autoBootstrap.organization }}
            - --k8-secret-template={{ $sanctumValues.autoBootstrap.secretTemplate }}
            - --ignore-if-bootstrapped=true
          envFrom:
            - secretRef:
                name: {{ $sanctumValues.autoBootstrap.credentialSecret.name }}
{{- end }}
